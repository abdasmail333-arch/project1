import pandas as pd

df = pd.read_csv("group01_chat_input.csv")

print("הודעות שכבת היישום")
print(df)

print("הסבר תהליך האריזה")
for _, row in df.iterrows():
    print("מספר הודעה:", row["msg_id"])
    print("שכבת היישום:", row["message"])
    print("שכבת התעבורה TCP")
    print("שכבת הרשת IP")
